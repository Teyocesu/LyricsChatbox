# Current handoff — v0.7.0 Phase 4.1 implemented; physical QA pending

- Branch: `codex/v0.7.0`; Phase 4.1 base `a75ec0dc4142371f63e14d3ebce701a79a01a0cb`; stable `main`/`origin/main`/`v0.6.2` remain `33838408f29b853c8945a0595fc4400420d41d27`.
- Scope: corrected only the first Phase 4 physical-QA findings. Phase 5/About/Output redesign, version bump, tag, release and main merge remain untouched.
- Rotation editor: explicit None/Adding/Editing state replaces coupled booleans/selection. Add success selects the new item before refresh, invalid Add preserves its draft, Delete leaves no selection, profile switches start unselected, and reorder/enable actions retain selection by ID. Removal is an accessible inline themed confirmation; rows use app theme resources rather than default WPF white surfaces.
- Display/Home: the Custom Decorations launcher now sits beside its label. Lyric Context cards remain present on Home and Display; unsupported compositions disable the controls and explain why while preserving ContextMode, so Home has no empty preference-card hole.
- Profiles: canonical built-in Status (`status`, Status / Time) is present in fresh migration and appended idempotently to existing valid libraries when capacity permits. Existing selection, profiles, customized built-ins and rotations are preserved; an existing `status` ID or a full 20-profile library is never overwritten or evicted.
- Verification: focused Display/Presentation/Rotation/OutputPause/DecorationPicker suite `168/168`; locked restore PASS; Release build 0 warnings/0 errors; full suite `481/481`, 0 skips; package policy and `git diff --check` PASS. Complete-diff review found no blocker, dependency, runtime-core change, native rotation dialog, hardcoded theme addition or Phase 5 leakage. Native Release smoke confirmed themed rotation rows, no implicit row selection/editor, the stable disabled Lyric Context card with explanatory copy, and Output Off.
- Next action: product owner performs the pending Phase 4.1 physical QA against the open Release app; do not begin Phase 5.
