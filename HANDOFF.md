# Current handoff

Read SPEC.md and PLAN.md. Active goal is v0.3 on codex/v0.3.0; v0.2 public release is untouched. The native WPF redesign and real NetEase fallback are implemented. User accepted charcoal/lavender direction, requested and received icon/slogan cleanup, then requested message alignment/ASCII templates. User confirmed Left/Center/Right ASCII in the actual VRChat headset.

Provider final-chain benchmark: 12 incremental timed results on confirmed LRCLIB misses/ambiguity; 11 primary-hit controls remain primary-only. NetEase is community REST, no cookies/credentials/scraping/runtime dependency; strict matching, 5s primary + 6s secondary deadlines, independent cooldown and cache provenance. Actual Apple Music live Ado recording loaded NetEase in the app. User did not observe audio alignment, so do not count it as physically synchronized. Terms/licensing uncertainty is documented; no official agreement asserted.

Current RC passed 90 tests and Release build with zero warnings/errors. Packaged executable and Custom editor were inspected; all four pages work at normal and minimum window sizes. Final code review completed. Pending: headset synchronization with the NetEase recording, final playback/manual regression acceptance, and high-DPI inspection where available. User requested publication only if everything is ready; that condition is not yet met. Keep final publication pending these checks. RC artifacts are local; package evidence is in PLAN.md. No subagents authorized. No final tag, push or release has been made.
