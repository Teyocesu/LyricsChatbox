# Current handoff — v0.7.0 Phase 5 implemented; physical QA pending

- Branch: `codex/v0.7.0`; stable `main`/`origin/main`/`v0.6.2` remain `33838408f29b853c8945a0595fc4400420d41d27`.
- Scope: Phase 4 is closed after product-owner physical approval of Phase 4.2. Phase 5 adds About as a persisted bottom-sidebar section, moves Updates/Open data folder/diagnostics to About without behavior change, cleans Settings to configurable behavior, and restyles the Output card around Off/Active/Paused states. No version bump, tag, release or main merge.
- About: identity from `ProductIdentity.DisplayVersion`, Made by Teyocesu, fixed GitHub/VRChat links, Discord `teyocesu` Copy-only, repository/releases/report-issue links, truthful local-first copy, bundled-notice opener with packaged-build fallback; failures report to a local About status line.
- Output: `RefreshOutputPauseView()` still authoritative via pure `OutputSidebarPresentation.Describe`; Off hides Pause/Resume and shows a scheduled-pause hint only as secondary text; pause modes and no-backlog contracts unchanged.
- Verification: locked restore PASS; Release build 0 warnings/0 errors; focused `98/98`; full suite `501/501`, 0 skips; package policy PASS under PowerShell 7; `git diff --check` PASS. Structural XAML inspection found one Updates and one diagnostics set in About and no Settings duplicates. Native Release app is open at PID `41292` with persisted Output Off; this is launch preparation, not physical acceptance.
- Next action: product owner performs Phase 5 physical QA against the open Release app. Do not begin release preparation.
