# Current handoff — v0.7.0 Phase 3 implemented; physical picker QA pending

- Branch: `codex/v0.7.0`; Phase 3 base `63c3d113899fb6168a020b609005282f8df02ea8`; stable `main`/`origin/main`/`v0.6.2` remain `33838408f29b853c8945a0595fc4400420d41d27`.
- Implemented: one owned modal Decorations picker for current Custom, Status and Manual editors; content-specific rendering; live search; Favorites; minimal My-item add/edit/delete; one pure caret/selection/`MaxLength` insertion helper; current-composition truncation diagnostics through `ChatboxFormatter.Analyze` as the sole 144/142/nine-line/grapheme authority.
- State: Phase 2 catalog and `decorations.json` are loaded once at `MainWindow` initialization and rebuilt only after explicit mutation. Failed saves retain understandable session state; opening/canceling does not write; unavailable built-ins do not hide valid My items.
- Removed: `CustomAsciiBox`, `StatusAsciiBox`, `ManualAsciiBox`, `InsertAscii`, `AsciiTemplate` and `MessageLayout.Templates`. No scheduler, output, OSC, dependency, network, version, README or legal-notice change.
- Verification: focused `50/50`; locked restore PASS; Release build 0 warnings/0 errors; full `447/447`, 0 skips; package policy and `git diff --check` PASS. Quality/security review found no execution/path/network boundary; Ponytail FULL review: `Lean already. Ship.`
- Pending gate: run physical WPF QA with Output OFF for Custom/Status/Manual insertion, search, Favorites, My-item CRUD, Esc/Enter/Ctrl+F/Tab/focus return, alternate accent and small-window scrolling. The available computer-use host exposed no native Windows app surface, so these are not claimed here.
- Next: complete that physical picker matrix, then Phase 4 Display redesign and rotating-message UI may begin. Product version stays `0.6.2`; do not tag, release or merge main.
