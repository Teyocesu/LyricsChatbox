# Current handoff — v0.7.0 Phase 4.2 implemented; physical QA pending

- Branch: `codex/v0.7.0`; Phase 4.1 base `a75ec0dc4142371f63e14d3ebce701a79a01a0cb`; stable `main`/`origin/main`/`v0.6.2` remain `33838408f29b853c8945a0595fc4400420d41d27`.
- Scope: corrected only the final Phase 4.1 physical-QA findings: disabled Lyric Context styling, canonical Status / Time profile normalization/order, and the Lyrics Details duration label. Phase 5/About/Output redesign, version bump, tag, release and main merge remain untouched.
- Rotation editor: explicit None/Adding/Editing state replaces coupled booleans/selection. Add success selects the new item before refresh, invalid Add preserves its draft, Delete leaves no selection, profile switches start unselected, and reorder/enable actions retain selection by ID. Removal is an accessible inline themed confirmation; rows use app theme resources rather than default WPF white surfaces.
- Display/Home: the Custom Decorations launcher now sits beside its label. Lyric Context cards remain present on Home and Display; unsupported compositions disable the controls and explain why while preserving ContextMode, so Home has no empty preference-card hole.
- Profiles: the five canonical built-ins are ordered Lyrics, Minimal, Music Info, Status / Time and Custom. Existing built-in `status` is renamed/moved before canonical Custom without resetting rotation or presentation settings; missing status is inserted there when capacity permits; user-owned profiles and full libraries remain intact.
- Lyrics Details: the row is labeled `Track / lyrics duration`, still showing source total / matched-candidate total; the obsolete footnote is gone and elapsed playback is not used.
- Verification: focused profile/presentation/duration suite `96/96`; locked restore PASS; Release build 0 warnings/0 errors; full suite `484/484`, 0 skips; package policy and `git diff --check` PASS. Complete-diff review found no dependency, runtime-core change, hardcoded theme color or Phase 5 leakage.
- Native QA preparation: Release app must remain open with Output Off. Physical product-owner QA is still pending; no visual PASS is claimed.
- Next action: product owner performs the pending Phase 4.2 physical QA against the open Release app; do not begin Phase 5.
