# Current handoff — v0.7.0 Phase 4 implemented; physical QA pending

- Branch: `codex/v0.7.0`; Phase 4 base `da38bfeeb7f981775d97e44d46d5dbdc926036b3`; stable `main`/`origin/main`/`v0.6.2` remain `33838408f29b853c8945a0595fc4400420d41d27`.
- Phase 3 closure: the product owner physically approved the final Phase 3.2 Decorations picker, including naming, navigation/default, My items, scroll reset, fit language, 327-item catalog, density/variety and interaction behavior.
- Implemented: Display now reads Profile → Composition → contextual Status messages/Lyric Context → Presentation → Live Preview. Custom exposes eight accessible token chips using shared `TextInsertion`; exact composer token support drives cold-start and live visibility. One automatic alignment control remains.
- Rotation: selected profiles own bounded immutable list edits and legacy first-enabled Message mirroring. One MainWindow `MessageRotator` feeds `{message}` before the existing composer/formatter/scheduler. Output Off, Output Pause, Manual ownership and absent `{message}` freeze remaining time; track/lyrics do not reset it; runtime changes never save.
- Preservation: presentation edits update the selected profile instead of reconstructing it, so Rotation survives presets, templates, alignment, Floating and Lyric Context. Duplicate copies configuration under a new ID/runtime state; rename keeps ID/state. Decorations reuse the approved picker and preview prospective output as if the edited message were active.
- Verification: focused Phase 4 suite `160/160`; locked restore PASS; Release build 0 warnings/0 errors; full suite `473/473`, 0 skips; package policy and `git diff --check` PASS; native Release smoke launch PASS with Output Off. Multi-axis/complete-diff review found no blocker and removed repeated steady-state rotator allocations; no dependency or Phase 5 change was added.
- Next action: product owner physically inspects the open Release build with Output Off. Product version stays `0.6.2`; Phase 5/About/Output redesign, version bump, tag, release and main merge remain untouched.
